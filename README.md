# Nexus CRM

Nexus CRM is a mobile customer relationship management application built with
Flutter for Android and iOS. It is designed to give administrators and sales
representatives a focused workflow for managing leads, clients, tasks, and
follow-ups.

## Project status

The project foundation and authentication foundation are complete.

The application currently supports:

- Firebase email/password sign-in and sign-out
- Restored Firebase sessions
- Single-workspace membership resolution
- Administrator and sales-representative role routing
- Explicit invited, suspended, revoked, and misconfigured access states
- Default-deny Firestore rules with focused emulator coverage
- Placeholder administrator and sales destinations

Sales-representative invitation and onboarding are intentionally deferred to a
later feature.

## MVP scope

- Authentication with role-based routing
- Sales representative management for administrators
- Sales representative dashboard
- Lead and client management
- Tasks and follow-ups
- Native phone dialer launch
- Manual post-call follow-up notes
- Basic administrator activity view

Advanced calendar features, push notifications, document management,
WhatsApp and email integrations, and detailed reporting are planned for later
iterations.

## Requirements

- Flutter SDK with Dart `^3.12.0`
- Android Studio and an Android SDK for Android development
- Xcode and CocoaPods for iOS development (minimum iOS version: 15)
- Node.js and npm for Firestore rules tests
- Java 21 for the Firebase Emulator Suite

Confirm the local toolchain before running the project:

```sh
flutter doctor
```

## Getting started

Install dependencies:

```sh
flutter pub get
```

Run the application on a connected device or simulator:

```sh
flutter run
```

The development Firebase project is `nexuscrm-dev-amitaswal`. Android and iOS
configuration is generated by FlutterFire and checked into the platform
projects. Do not commit Firebase user passwords or service-account
credentials.

See [Authentication architecture](docs/authentication.md) for the data model,
security model, development seeding, and deferred onboarding flow.

## Quality checks

Run static analysis:

```sh
flutter analyze
```

Run the test suite:

```sh
flutter test
```

Install and run the Firestore rules tests:

```sh
npm install
npm run test:firestore-rules
```

Deploy reviewed Firestore rules and indexes to the development project:

```sh
firebase deploy --only firestore:rules,firestore:indexes \
  --project nexuscrm-dev-amitaswal
```

## Branching strategy

- `main` contains stable, release-ready work.
- `dev` is the integration branch for completed features.
- `feature/*` branches are created from `dev` for focused changes.

## Project structure

```text
lib/
├── app/
│   ├── router/
│   ├── theme/
│   ├── widgets/
│   └── app.dart
├── features/
│   ├── admin/
│   ├── authentication/
│   │   ├── data/
│   │   ├── domain/
│   │   └── presentation/
│   └── sales/
├── firebase_options.dart
└── main.dart
test/
├── app/
├── features/
├── app_test.dart
└── firestore_rules_test.js
```

Application identifier: `com.amitaswal.nexuscrm`
