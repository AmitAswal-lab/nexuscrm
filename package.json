{
  "name": "nexuscrm-firebase-tests",
  "private": true,
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-nexuscrm --only firestore \"node --test test/firestore_rules_test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
