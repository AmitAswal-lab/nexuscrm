{
  "name": "nexuscrm-firebase-tests",
  "private": true,
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-nexuscrm --only firestore \"node --test test/firestore_rules_test.js\"",
    "test:functions": "npm --prefix functions run build && firebase emulators:exec --project demo-nexuscrm --only auth,firestore,functions \"npm --prefix functions run test:emulator\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
